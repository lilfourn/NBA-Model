"""Utility scripts for the project.

The top-level `scripts/*.py` files are kept as stable entrypoints.
New code should live in categorized subpackages (e.g. `scripts.prizepicks`, `scripts.nba`).
"""

